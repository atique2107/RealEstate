/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        unoptimized:true,
        domains:['pmgueanqjrgwhaysyftp.supabase.co','img.clerk.com']
    }
};

export default nextConfig;
